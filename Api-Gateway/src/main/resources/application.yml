spring:
  config:
    import: "optional:file:.env[.properties],optional:file:./Api-Gateway/.env[.properties]"
  application:
    name: Api-Gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: ${DB_CONNECTION_TIMEOUT:30000}
      maximum-pool-size: ${DB_MAX_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:5}
      idle-timeout: ${DB_IDLE_TIMEOUT:600000}
      max-lifetime: ${DB_MAX_LIFETIME:1800000}
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: false
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true
    show-sql: ${JPA_SHOW_SQL:false}
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:10MB}
      max-request-size: ${MAX_REQUEST_SIZE:10MB}
      enabled: true

  # ── Spring Cloud Gateway MVC ─────────────────────────────────────────────
  # Routes all downstream traffic.  Auth/user endpoints are handled locally
  # by this service itself (no route needed).
  #
  # StripPrefix=1 removes the service prefix before forwarding:
  #   /market/listings  →  Market-Access-App:/listings
  #
  # CircuitBreaker forwards to /fallback/* when a service is down.
  cloud:
    gateway:
      mvc:
        routes:
          - id: contract-farming
            uri: lb://Contract-Farming-App
            predicates:
              - Path=/contract-farming/**
            filters:
              - StripPrefix=1
              - name: CircuitBreaker
                args:
                  name: contractFarming
                  fallbackUri: forward:/fallback/contract-farming

          - id: market-access
            uri: lb://Market-Access-App
            predicates:
              - Path=/market/**
            filters:
              - StripPrefix=1
              - name: CircuitBreaker
                args:
                  name: marketAccess
                  fallbackUri: forward:/fallback/market

          - id: generate-agreement
            uri: lb://Generate-Agreement-App
            predicates:
              - Path=/agreement/**
            filters:
              - StripPrefix=1
              - name: CircuitBreaker
                args:
                  name: generateAgreement
                  fallbackUri: forward:/fallback/agreement

# ── CORS ─────────────────────────────────────────────────────────────────────
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5000,http://localhost:5174,http://localhost:1819}

# ── Server ───────────────────────────────────────────────────────────────────
server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
  http2:
    enabled: true

# ── Eureka ───────────────────────────────────────────────────────────────────
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
    registry-fetch-interval-seconds: 30
  instance:
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}

# ── Twilio ────────────────────────────────────────────────────────────────────
twilio:
  account-sid: ${TWILIO_ACCOUNT_SID}
  auth-token: ${TWILIO_AUTH_TOKEN}
  phone-number: ${TWILIO_PHONE_NUMBER}

# ── JWT ──────────────────────────────────────────────────────────────────────
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:604800000}

# ── Circuit Breaker (Resilience4j) ───────────────────────────────────────────
resilience4j:
  circuitbreaker:
    instances:
      contractFarming:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 15s
        failureRateThreshold: 50
        automaticTransitionFromOpenToHalfOpenEnabled: true
      marketAccess:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 15s
        failureRateThreshold: 50
        automaticTransitionFromOpenToHalfOpenEnabled: true
      generateAgreement:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        waitDurationInOpenState: 15s
        failureRateThreshold: 50
        automaticTransitionFromOpenToHalfOpenEnabled: true
  timelimiter:
    instances:
      contractFarming:
        timeoutDuration: 10s
      marketAccess:
        timeoutDuration: 10s
      generateAgreement:
        timeoutDuration: 10s

# ── Swagger / OpenAPI ─────────────────────────────────────────────────────────
# Single Swagger UI that aggregates API docs from all services.
# Proxy routes above expose each service's /v3/api-docs endpoint through this gateway.
# Access at: http://localhost:8080/swagger-ui.html
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    try-it-out-enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
    urls:
      - name: "API Gateway (Auth & Users)"
        url: /v3/api-docs
      - name: "Market Access (Listings & Images)"
        url: /market/v3/api-docs
      - name: "Contract Farming (Agreements, Orders, Payments)"
        url: /contract-farming/v3/api-docs
      - name: "Agreement Generator (Contracts & Cold Storage)"
        url: /agreement/v3/api-docs

# ── Actuator ─────────────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  appoptics:
    metrics:
      export:
        enabled: true

# ── Logging ──────────────────────────────────────────────────────────────────
logging:
  level:
    root: ${LOG_LEVEL_ROOT:INFO}
    com.agriconnect: ${LOG_LEVEL_APP:DEBUG}
    org.springframework.web: ${LOG_LEVEL_SPRING_WEB:INFO}
    org.springframework.security: ${LOG_LEVEL_SPRING_SECURITY:INFO}
    org.hibernate: ${LOG_LEVEL_HIBERNATE:WARN}
    org.springframework.cloud.gateway: ${LOG_LEVEL_GATEWAY:INFO}
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
